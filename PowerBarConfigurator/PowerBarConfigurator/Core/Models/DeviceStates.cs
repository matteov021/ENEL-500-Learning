namespace PowerBarConfigurator.Core.Models
{
    // Device connection states
    public enum ConnectionState
    {
        Disconnected,
        Connecting,
        Connected
    }

    // Device power states
    public enum PowerState
    {
        Idle,
        Active
    }
}